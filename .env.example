NEXT_PUBLIC_SITE_NAME=IT Quest
NEXT_PUBLIC_SITE_TAGLINE=Level Up Your Workload
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
NEXT_PUBLIC_SUPPORT_EMAIL=support@itquest.local

# Server-only: must match Backend/.env JWT_SECRET so proxy.ts can verify the session cookie
JWT_SECRET=replace-with-a-long-random-string